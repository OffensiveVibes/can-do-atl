import { CSSProperties, useEffect, useState } from "react";
import { trpc } from "@/lib/trpc";
import { ArrowRight, CalendarDays, ChevronLeft, ChevronRight, Facebook, HandHeart, Heart, Instagram, Linkedin, MapPin, Music2, Package, Shirt, Soup, Youtube } from "lucide-react";
import { Carousel, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from "@/components/ui/carousel";

const volunteerUrl = "https://forms.gle/ZnfzPQnNQNVfWABXA";
const essentialsUrl = "mailto:candoatltm@gmail.com?subject=Can%20Do%20ATL%20Essentials%20Donation";
const socialLinks = [
  { label: "LinkedIn", href: "https://www.linkedin.com/company/can-do-atl", icon: Linkedin },
  { label: "TikTok", href: "https://www.tiktok.com/@candoatl", icon: Music2 },
  { label: "Instagram", href: "https://www.instagram.com/candoatl/", icon: Instagram },
  { label: "Facebook", href: null, icon: Facebook },
  { label: "YouTube", href: "https://www.youtube.com/@CanDoATL", icon: Youtube },
];
const initiativeContent = [
  { key: "food_drives", number: "01", title: "Food Drives", copy: "Stock campus-facing shelves with groceries students can use right away.", icon: Soup, image: "/manus-storage/cando-food-drive_871f5113.png", hoverImage: "/manus-storage/cando-hover-food-drive_149f6ede.png" },
  { key: "clothing_closet", number: "02", title: "Clothing Closet", copy: "Share free, quality clothes for class, work, interviews, and everyday life.", icon: Shirt, image: "/manus-storage/cando-clothing-closet_0be9460f.png", hoverImage: "/manus-storage/cando-hover-clothing-closet_fd9db982.png" },
  { key: "community_outreach", number: "03", title: "Community Outreach", copy: "Meet students where they are with pop-ups, partner care packages, and practical resources.", icon: HandHeart, image: "/manus-storage/cando-outreach_ef7007e4.png", hoverImage: "/manus-storage/cando-hover-outreach_830f5058.png" },
];
const careNotes = [
  { label: "Care note 01", title: "A full shelf can steady a week.", copy: "Food drives turn everyday supplies into breathing room—one grocery bag, one meal, one less worry at a time.", context: "Food access, made tangible." },
  { label: "Care note 02", title: "A good outfit can change how the day begins.", copy: "Closet days keep quality clothing in circulation for class, work, interviews, and all the in-between moments.", context: "Clothing access, shared freely." },
  { label: "Care note 03", title: "Showing up is a skill we share.", copy: "Packed bags, sorted racks, and welcoming tables begin when students decide to make room for one another.", context: "Mutual aid, in motion." },
];
const fallbackImpactStats = [{ value: 0, label: "care packages shared so far — you can make the difference" }, { value: 0, label: "clothing items recirculated so far — you can make the difference" }, { value: 0, label: "student volunteers so far — you can make the difference" }];
const fallbackAppearance = { pageMode: "solid", pageColor: "#F7F3EB", pageGradientFrom: "#F7F3EB", pageGradientTo: "#E7EDE1", pageImageUrl: null, pageImageBlur: 0, pageOverlayOpacity: 24, headerMode: "solid", headerColor: "#F7F3EB", headerGradientFrom: "#F7F3EB", headerGradientTo: "#F7F3EB", headerImageUrl: null, headerImageBlur: 0, headerOverlayOpacity: 8, footerMode: "solid", footerColor: "#2C2C2C", footerGradientFrom: "#2C2C2C", footerGradientTo: "#3A5A40", footerImageUrl: null, footerImageBlur: 0, footerOverlayOpacity: 36 };
const starterSlides = [{ id: 0, eyebrow: "Student-led mutual aid across ATL", headline: "Small supplies.", accent: "Shared strength.", body: "Can Do ATL brings students together to combat food and clothing insecurity across Georgia Tech, Georgia State, and Kennesaw State.", imageUrl: "/manus-storage/cando-hero-atl_b12524b6.png", imageAlt: "Students sharing food, clothing, and care supplies", volunteerHref: volunteerUrl, donateHref: essentialsUrl }];

type Appearance = typeof fallbackAppearance;
function PaperTape({ className = "" }: { className?: string }) { return <span aria-hidden="true" className={`paper-tape ${className}`} />; }
function formatEventDate(date: Date) { return new Date(date).toLocaleDateString(undefined, { month: "short", day: "numeric" }).toUpperCase(); }

function Surface({ appearance, area, className = "", children }: { appearance: Appearance; area: "page" | "header" | "footer"; className?: string; children: React.ReactNode }) {
  const cap = area[0].toUpperCase() + area.slice(1) as "Page" | "Header" | "Footer";
  const mode = appearance[`${area}Mode` as keyof Appearance] as string;
  const color = appearance[`${area}Color` as keyof Appearance] as string;
  const from = appearance[`${area}GradientFrom` as keyof Appearance] as string;
  const to = appearance[`${area}GradientTo` as keyof Appearance] as string;
  const image = appearance[`${area}ImageUrl` as keyof Appearance] as string | null;
  const blur = appearance[`${area}ImageBlur` as keyof Appearance] as number;
  const opacity = appearance[`${area}OverlayOpacity` as keyof Appearance] as number;
  const style = { "--surface-color": color, "--surface-gradient": `linear-gradient(135deg, ${from}, ${to})`, "--surface-image": image ? `url("${image}")` : "none", "--surface-blur": `${blur}px`, "--surface-overlay": `${opacity / 100}` } as CSSProperties;
  return <div style={style} className={`dynamic-surface dynamic-${area} surface-${mode} ${className}`} data-surface={cap}>{children}</div>;
}

function TimedHero({ slides }: { slides: any[] }) {
  const liveSlides = slides.length ? slides : starterSlides;
  const [activeIndex, setActiveIndex] = useState(0);
  useEffect(() => setActiveIndex(0), [liveSlides.length]);
  useEffect(() => { if (liveSlides.length < 2) return; const id = window.setInterval(() => setActiveIndex((index) => (index + 1) % liveSlides.length), 4000); return () => window.clearInterval(id); }, [liveSlides.length]);
  const slide = liveSlides[activeIndex % liveSlides.length];
  return <section className="hero-section"><div className="site-shell hero-grid"><div className="hero-copy reveal-up"><div className="eyebrow eyebrow-ink"><span className="eyebrow-dot" /> {slide.eyebrow}</div><h1>{slide.headline}{slide.accent && <><br /><span>{slide.accent}</span></>}</h1><p className="hero-text">{slide.body}</p><div className="hero-actions"><a className="button button-primary" href={slide.volunteerHref || volunteerUrl}><HandHeart size={19} /> Volunteer with us <ArrowRight size={18} /></a><a className="button button-secondary" href={slide.donateHref || essentialsUrl}><Heart size={19} fill="currentColor" /> Give support</a></div><p className="hero-note">Bring what you can. Take what helps.</p><div className="campus-trail"><span><b>01</b> Georgia Tech</span><i /><span><b>02</b> Georgia State</span><i /><span><b>03</b> Kennesaw State</span></div></div><div className="hero-visual reveal-up" aria-live="off"><div className="hero-orbit orbit-one" /><div className="hero-orbit orbit-two" /><PaperTape className="hero-tape" /><img src={slide.imageUrl} alt={slide.imageAlt} className="hero-illustration" /><div className="hero-pin pin-top"><span>{liveSlides.length > 1 ? `${activeIndex + 1} / ${liveSlides.length}` : "3 campuses"}</span><MapPin size={16} /></div><div className="hero-pin pin-bottom"><span className="pin-burst">✦</span><span>Care in motion</span></div>{liveSlides.length > 1 && <div className="hero-carousel-controls"><button onClick={() => setActiveIndex((activeIndex - 1 + liveSlides.length) % liveSlides.length)} aria-label="Previous hero story"><ChevronLeft size={18} /></button><div>{liveSlides.map((item, index) => <button key={item.id} aria-label={`Show hero story ${index + 1}`} onClick={() => setActiveIndex(index)} className={index === activeIndex ? "active" : ""} />)}</div><button onClick={() => setActiveIndex((activeIndex + 1) % liveSlides.length)} aria-label="Next hero story"><ChevronRight size={18} /></button></div>}</div></div></section>;
}

function InitiativeCard({ item, visual, index }: { item: typeof initiativeContent[number]; visual: any; index: number }) {
  const [showHover, setShowHover] = useState(false);
  const Icon = item.icon;
  const primary = visual?.imageUrl || item.image;
  const hover = visual?.hoverImageUrl || item.hoverImage;
  return <article className={`initiative-card card-${index + 1} ${showHover ? "is-touch-swapped" : ""}`} onPointerEnter={() => setShowHover(true)} onPointerLeave={() => setShowHover(false)}><div className="initiative-topline"><span>{item.number}</span><Icon size={21} strokeWidth={1.8} /></div><button className="initiative-art-switch" type="button" onClick={() => setShowHover((value) => !value)} aria-pressed={showHover} aria-label={`Show alternate ${item.title} image`}><img src={primary} alt={visual?.imageAlt || item.title} className="initiative-art initiative-art-primary" /><img src={hover} alt="" aria-hidden="true" className="initiative-art initiative-art-hover" /><span>Tap for another moment</span></button><div className="initiative-content"><h3>{item.title}</h3><p>{item.copy}</p><a href="#get-involved" className="text-link">Lend a hand <ArrowRight size={15} /></a></div></article>;
}

export default function Home() {
  const content = trpc.content.public.useQuery();
  const appearance = (content.data?.appearance ?? fallbackAppearance) as Appearance;
  const events = content.data?.events ?? [];
  const updates = content.data?.updates ?? [];
  const impactStats = content.data?.impactMetrics ?? fallbackImpactStats;
  const serviceCards = content.data?.serviceCards ?? [];
  const slides = content.data?.slides ?? [];
  return <Surface appearance={appearance} area="page" className="site-page"><header className="site-header"><Surface appearance={appearance} area="header" className="site-header-surface"><div className="site-shell header-inner"><a className="brand-lockup" href="#top"><span className="brand-mark-wrap"><img src="/manus-storage/cando-mark_4f760226.png" alt="" className="brand-mark" /></span><span className="brand-name"><strong>Can Do</strong><em>ATL</em></span></a><nav className="desktop-nav"><a href="#what-we-do">What we do</a><a href="#impact">Impact</a><a href="/about">About us</a><a href="#get-involved">Get involved</a></nav></div></Surface></header><main id="top"><TimedHero slides={slides} /><section id="what-we-do" className="initiatives-section section-pad"><div className="site-shell"><div className="section-heading reveal-up"><div><p className="kicker">What we do</p><h2>Care is practical.</h2></div><p>We mobilize people, supplies, and campus partnerships so a student’s next step can feel a little more possible.</p></div><div className="initiative-grid">{initiativeContent.map((item, index) => <InitiativeCard key={item.key} item={item} index={index} visual={serviceCards.find((card: any) => card.cardKey === item.key)} />)}</div></div></section><section id="impact" className="impact-section section-pad"><div className="impact-grain" /><div className="site-shell impact-layout"><div className="impact-intro"><p className="kicker kicker-light">Our impact</p><h2>Every item carries a little more room to breathe.</h2><p>When a student has food on the table or clothes that feel ready for the day, a community grows stronger.</p><span className="sample-badge">A growing community, one drive at a time</span><div className="impact-campus-trail"><span>Georgia Tech</span><i /><span>Georgia State</span><i /><span>Kennesaw State</span></div></div><div className="stats-board">{impactStats.map((stat: any) => <div className="stat-note" key={stat.label}><strong>{stat.value}</strong><span>{stat.label}</span><i /></div>)}</div></div></section><section className="drives-section section-pad"><div className="site-shell drives-layout"><div className="drives-sticky-note"><PaperTape className="drives-tape" /><div className="calendar-icon"><CalendarDays size={20} /></div><p className="kicker">Mark your calendar</p><h2>Meet us where care is needed.</h2><p>Plan around the next chance to pack, sort, share, and make a useful difference close to campus.</p></div><div className="drive-list">{events.length ? events.map((event: any) => <article className="drive-row" key={event.id}><time>{formatEventDate(event.startsAt)}</time><div><h3>{event.title}</h3><p><MapPin size={15} /> {event.campus}</p></div><a href={event.linkHref || "#get-involved"}><ArrowRight size={21} /></a></article>) : <div className="public-empty-event"><CalendarDays size={23} /><p>New events will appear here as soon as they are announced.</p></div>}</div></div></section>{updates.length > 0 && <section className="updates-section"><div className="site-shell"><div className="updates-heading"><p className="kicker">From the community board</p><h2>What’s moving now.</h2></div><div className="public-updates">{updates.map((update: any) => <article key={update.id}><span>Field note</span><h3>{update.title}</h3><p>{update.body}</p>{update.linkHref && <a href={update.linkHref}>{update.linkLabel || "Learn more"} <ArrowRight size={15} /></a>}</article>)}</div></div></section>}<section className="stories-section section-pad"><div className="site-shell stories-layout"><div className="stories-heading"><p className="kicker">The way we show up</p><h2>Care moves through people.</h2><p>Food and clothing access is never a one-person fix. Here is the shared work that keeps care moving across our campuses.</p><div className="hand-drawn-arrow">↘</div></div><Carousel opts={{ loop: true, align: "start" }} className="stories-carousel"><CarouselContent>{careNotes.map((story, index) => <CarouselItem key={story.label}><article className={`story-card story-${index + 1}`}><div className="story-card-topline"><span>{story.label}</span><i>✦</i></div><h3>{story.title}</h3><p>{story.copy}</p><footer><span>{story.context}</span></footer></article></CarouselItem>)}</CarouselContent><div className="story-controls"><CarouselPrevious className="story-control-prev" /><CarouselNext className="story-control-next" /></div></Carousel></div></section><section id="get-involved" className="action-section section-pad"><div className="site-shell action-panel"><div className="action-copy"><p className="kicker kicker-light">Make room for someone else</p><h2>What can you carry forward?</h2><p>Show up for a shift, organize a drive, pass along essentials, or help us keep this campus trail moving.</p></div><div className="action-options"><a className="action-option volunteer-option" href={volunteerUrl} target="_blank" rel="noreferrer"><span className="option-icon"><HandHeart size={27} /></span><span><strong>Volunteer with us</strong><small>Sort, deliver, organize, and welcome</small></span><ArrowRight size={23} /></a><a className="action-option donate-option" href={essentialsUrl}><span className="option-icon"><Package size={27} /></span><span><strong>Give essentials</strong><small>Fuel food and clothing access</small></span><ArrowRight size={23} /></a></div></div></section></main><Surface appearance={appearance} area="footer" className="site-footer-surface"><footer id="contact" className="site-footer"><div className="site-shell footer-main"><div className="footer-brand"><a className="brand-lockup footer-logo" href="#top"><span className="brand-mark-wrap"><img src="/manus-storage/cando-mark_4f760226.png" alt="" className="brand-mark" /></span><span className="brand-name"><strong>Can Do</strong><em>ATL</em></span></a><p>Three campuses, one connected care network, and a shared belief that essentials should never stand in the way of a student’s next step.</p></div><div className="footer-column"><p className="footer-label">Our campuses</p><ul><li>Georgia Tech</li><li>Georgia State</li><li>Kennesaw State</li></ul></div><div className="footer-column footer-socials"><p className="footer-label">Find &amp; share us</p><div className="social-grid">{socialLinks.map((social) => { const Icon = social.icon; return social.href ? <a key={social.label} href={social.href} target="_blank" rel="noreferrer"><Icon size={19} /><span>{social.label}</span></a> : <span key={social.label} className="social-placeholder"><Icon size={19} /><span>{social.label}</span><small>Soon</small></span>; })}</div><p className="social-helper">Follow the work. Share a drive. Keep the trail moving.</p></div></div><div className="site-shell footer-bottom"><span>© {new Date().getFullYear()} Can Do ATL</span><a className="footer-staff-signin" href="/admin">Staff sign in <span>☺</span></a><span>Built for shared strength.</span></div></footer></Surface></Surface>;
}
